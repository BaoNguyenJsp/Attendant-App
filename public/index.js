/* =====================================================================
   SỔ THIẾU NHI — index.js (launcher)
   Chỉ cần initCommon(): shell (header + year bar + toast), đổi năm học
   và hiển thị/ẩn liên kết theo vai trò đã được xử lý trong common.js.
   ===================================================================== */
'use strict';

import { initCommon } from './shared/common.js';

await initCommon();
